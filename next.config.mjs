/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['via.placeholder.com', 'gravatar.com', 'lh3.googleusercontent.com', 'res.cloudinary.com'],
    },
};

export default nextConfig;
